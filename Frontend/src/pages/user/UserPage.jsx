import { Flex } from "@mantine/core";
import { UserInfo } from "../../components/user/UserInfo";

export const UserPage = () => {
  return (
    <Flex justify={"center"}>
      <UserInfo />
    </Flex>
  );
};
